<!DOCTYPE html>
<html>
<head>
    <title>Liste des cars</title>
</head>
<body>
<h1>Détails de l'car</h1>

<table>
    <tr>
        <th>ID</th>
        <td>{{ rental.id }}</td>
    </tr>
    <tr>
        <th>rentaldate</th>
        <td>{{ rental.rentaldate|date('Y-m-d') }}</td>
    </tr>
    <tr>
        <th>returndate</th>
        <td>{{ rental.returndate|date('Y-m-d') }}</td>
    </tr>
    <tr>
        <th>returnactuel</th>
        <td>{{ rental.actuelreturndate|date('Y-m-d') }}</td>
    </tr>
    <tr>
        <th>retard</th>
        <td>{{ rental.islate ? 'Yes' : 'No' }}</td>
    </tr>
    <tr>
        <th>car id</th>
        <td>{{ rental.car.id }}</td>
    </tr>
    <tr>
        <th>car model</th>
        <td>{{ rental.car.model }}</td>
    </tr>
    <tr>
        <th>car brand</th>
        <td>{{ rental.car.brand }}</td>
    </tr>
    <tr>
        <th>customer id</th>
        <td>{{ rental.customer.id }}</td>
    </tr>
    <tr>
        <th>customer name</th>
        <td>{{ rental.customer.name }}</td>
    </tr>
   

    <!-- Ajoutez d'autres champs selon votre entité customer -->
</table>
</body>